{
  "name": "openai-demo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.3.0",
    "openai": "4.28.4",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.5.4"
  },
  "engines": {
    "node": "18"
  }
}
